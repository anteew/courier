// Placeholder Kernel adapter. In v1, run Courier as standalone HTTP/WS server and
// register its capabilities via Hostess when embedded. This file will import
// Kernel from vendor/mkolbol once packaged.
export function registerWithKernel() {
  // TODO: integrate with mkolbol Kernel (transform service registration)
}
