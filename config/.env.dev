# Courier Development Environment Configuration
# Copy this file to .env and adjust as needed

# Server configuration
COURIER_HTTP_PORT=8787
COURIER_WS_PORT=8788
COURIER_BIND=127.0.0.1

# Disable gateways (useful for testing)
COURIER_DISABLE_HTTP=false
COURIER_DISABLE_WS=false

# Rate limiting (disabled by default for development)
COURIER_RATE_LIMIT=false
COURIER_RATE_LIMIT_PER_IP=100
COURIER_RATE_LIMIT_WINDOW_MS=60000

# Token allowlist (disabled by default; comma-separated)
# When set, enforces Bearer token auth on protected endpoints
# COURIER_TOKENS=dev-local,demo-token

# Control protocol recording (leave empty to disable)
COURIER_RECORD_CONTROL=

# Triggers configuration file
COURIER_CONFIG=config/dev.yaml

# Examples for local development:

# Enable rate limiting for demo
# COURIER_RATE_LIMIT=true
# COURIER_RATE_LIMIT_PER_IP=10
# COURIER_RATE_LIMIT_WINDOW_MS=1000

# Record control frames for debugging
# COURIER_RECORD_CONTROL=/tmp/courier-control.jsonl

# Enable token auth for testing
# COURIER_TOKENS=dev-local
